//! Recording writer namespace.
//!
//! The writer implementation remains in `recording::mod` for now; this module
//! gives new code a stable owner namespace as the runtime/writer split
//! continues.

pub use super::RecordingStart;
